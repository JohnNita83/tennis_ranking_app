{
  "name": "Flask Dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [5000],
  "postCreateCommand": "pip install -r requirements.txt",
  "runArgs": ["-v", "/workspace:/workspace:cached"],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
